---
- name: Restart grafana
  ansible.builtin.systemd:
    name: grafana
    state: restarted
    enabled: true

- name: Reload grafana
  ansible.builtin.systemd:
    name: grafana
    state: reloaded
